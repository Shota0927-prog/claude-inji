//@version=6
// W05 B14R R0C: consumer imports the POC Main only (no direct Worker import).
indicator("ZoneEngineV2 W05 Candidate POC Consumer", overlay = false)

import sekine3310/ZoneEngineV2_W05Candidate_POC_Main/1 as W05Main

var W05Main.HostState host = W05Main.newHost()

int logicalCountValue = W05Main.logicalCount(host)
int markerValue       = W05Main.markerValue(host)

plot(logicalCountValue, "logicalCount")
plot(markerValue, "marker")
